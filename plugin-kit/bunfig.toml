# Resolve the `@slipstream` scope from the GitHub npm registry. During local development the
# SDK is consumed via the `link:../sdk` devDependency; this matters once the kit is depended
# on from outside the repo.
[install.scopes]
"@slipstream" = "https://github.com/vindeckyy/slipstream/api/packages/unom/npm/"
