//! Scaffold message for the glass-to-glass latency probe.

pub fn announce() {
    println!(
        "latency-probe: scaffold (render timestamp/QR on host, read back on client; track p50/p99)"
    );
}
